interface INavItems {
    name: string;
    path: string;
}

export const navItems: INavItems[] = [
    { name: "Home", path: "/home" },
    { name: "Features", path: "/features" },
    { name: "Contact", path: "/contact" },
];